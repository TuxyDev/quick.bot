const Discord = require('discord.js');
const client = new Discord.Client();


class Bot {

    /**
     * 
     * @param {<Bot Token>} token   the token from the applications page.
     * @param {String} prefix       the prefix to use for the bot.
     */ 

     
    constructor(token, prefix) {
        
    }
}

module.exports = Bot;